"""Phase 15 auth package. See docs/PHASE15_AUTH.md.

Sub-modules are intentionally flat for M1 (fewer files > premature packages).
Split into authz/, security/, notify/ if this grows past ~10 files.
"""
